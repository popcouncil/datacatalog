<% content_for :title do %>Browse<% end %>
<% content_for :body_class do %>browse<% end %>
<div id="column_fullWidth">
  <%#= render :partial => 'filters' %>
  <%= show_flash_message %>

  <div class="tableNav">
    <%= will_paginate @data_records %>
  </div>

  <%= render 'shared/source_table' %>

  <div class="tableNav">
    <%= will_paginate @data_records %>
  </div>
</div>

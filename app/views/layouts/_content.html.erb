<div id="contentHeader">
  <% if params[:action] == 'dashboard' && !current_user %>
    <%= yield(:feature_box) %>
  <% else %>
    <h2><%= yield(:title) %><%= yield(:title_suffix) %></h2>
    <%= yield(:tabs) %>
  <% end %>
</div>

<%= yield %>

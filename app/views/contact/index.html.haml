- content_for :title do
  Contact Us
.block-about
  %h1 Contact Us
  = show_flash_message
  .block-contact
    - form_for @contact_submission, :url => contact_submission_path, :html => {:class => 'form-contact'} do |form|
      %fieldset
        %legend Title
        .title
          %p Have a question or comment about the site? Submit it below.
          %p= form.error_messages custom_error_messages
        .row
          %label{:for => "txt01"} If you have a question about a particular data record in the Catalog, use the Comment or Report a Problem tool on that data record's page.
          .texti
            - if current_user
              = form.hidden_field :user_id, :value => current_user.id
            = form.text_field :name
          .texti
            = form.text_field :email
          .text
            = form.text_area :comments, :id => 'txt01', :cols => 30, :rows => 10
          %input.btn-submit{:type => "submit", :value => "Submit"}/

